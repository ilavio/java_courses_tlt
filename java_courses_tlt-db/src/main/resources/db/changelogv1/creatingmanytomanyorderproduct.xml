<?xml version="1.0" encoding="UTF-8"?>
<databaseChangeLog
    xmlns="http://www.liquibase.org/xml/ns/dbchangelog"
    xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
    xsi:schemaLocation="http://www.liquibase.org/xml/ns/dbchangelog
        http://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-3.0.xsd">

    <changeSet id="2020-05-18" author="iliya_potemkin">
        <comment>
            Создание таблицы OrderProduct
        </comment>
        <sql>
            CREATE TABLE potemkin.OrderProduct
            (
            ProductId INTEGER REFERENCES potemkin.product (productid)
            ON delete cascade
            ON update cascade,
            OrderId INTEGER REFERENCES potemkin.orders (orderid)
            ON delete cascade
            ON update cascade
            );
        </sql>
    </changeSet>

</databaseChangeLog>